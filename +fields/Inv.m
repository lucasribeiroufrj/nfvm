function newField = Inv(field)
% inv Matrix inverse.
%   inv(field) returns a new scalar field, which each element of this
%   field is the matrix inverse of the corresponding element of field input.

newField = field.inv();

end

